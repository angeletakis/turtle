//
// Created by alex on 11/11/21.
//
#include "node.hpp"

/*
using namespace turtle;
constexpr turtle::turtle_flag_t node_t::type() const {
  return NodeFlags >> tokenTypeOffset;
}

constexpr turtle::turtle_flag_t node_t::type_of(turtle::turtle_flag_t _f) {
  return _f >> tokenTypeOffset;
}

constexpr turtle::turtle_flag_t node_t::test_bit(unsigned char i) const {
  return (NodeFlags & ((turtle::turtle_flag_t)1 << i));
}

constexpr turtle::turtle_flag_t node_t::hasType(turtle::turtle_flag_t _f) const
{ return type() == _f;
}

// extract bits then check flag tegridy
constexpr turtle::turtle_flag_t node_t::hasFlag(const turtle::turtle_flag_t _f)
{ return (NodeFlags & _f) == _f && hasType(type_of(_f));
}*/
